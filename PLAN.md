# BK4802 + STM32 + ST7567 LCD12864 「APRS 寻呼机」改装计划

> 参考资料：MM-Radio (doublehan07)、BG7QKU STM32_SIMPLE_CONTROL_BK4802N、BG5ESN FMO BK4802 V2.00、VP-Digi (sq8vps)。
> 参考代码已浅克隆到临时目录 `%TEMP%\mm_ref_bk4802` 仅供只读研究；本文件不含任何执行产物。

> **项目名（暂定）：`BBcall_APRS`** —— 复刻 BB 机、用 APRS 实现的无线电寻呼机。

## 1. 目标与产品定义

把一台 BK4802 儿童对讲机板（参考 MM-Radio 的「麦门对酱机」魔改思路）改造成一台**复刻 BB 机（无线寻呼机）**，但通信协议不用 POCSAG，而是 **APRS（AX.25 / 1200 baud AFSK，Bell 202）**。

**寻呼机行为**（对标老式汉显 BB 机）：

- 接收 APRS 消息帧（`<源呼号>:<addressee>:<正文>`）→ 蜂鸣/振动 + LED 闪烁 + 屏幕点亮
- 屏幕显示：发送方呼号、时间、正文、信号强度、当前频率
- 待机界面：频率 + 时间 + 信号条（类似 BB 机守候界面）
- 上/下键翻阅历史消息；最近 N 条非易失存储
- 可选按呼号/消息地址过滤，仅提示本机相关消息

**关键设定：仅接收（RX-only）。** 与 MM-Radio 一致，不做发射。接收无需执照；若未来要发（位置信标/应答）再另行评估，涉及业余无线电执照与 BK4802 发射通路。

**本轮已拍板（2026-09-08）：**

- LCD 模组：**ST7567**（纯图形、无内置字库 → MCU 需自备点阵字库）。
- 主控：**STM32F103C8T6**（64KB Flash / 20KB SRAM）。
- 音频引出：**从 FM 解调输出引出**（功放前级、解调后），利于 AFSK 判频。
- 能力边界：**仅接收，禁用发射**（reg13 强制 RX 模式，PTT 只提示不动发射）。
- 默认频率：**2m 频段，144.640MHz**（APRS VHF）。

## 2. 参考项目与可复用结论

| 参考 | 复用什么 | 用途 |
|---|---|---|
| **MM-Radio** (doublehan07) | 板子拆解、滤波器/天线通路短接（飞线或拆 LC）、BK4802 驱动骨架（GPIO 模拟 I2C 时序） | 硬件改造流程直接沿用 |
| **BG7QKU STM32_SIMPLE_CONTROL_BK4802N** | 寄存器取值、频率写入方法（`0x90`/`0x48` I2C 地址，reg0/1/2 写频率） | BK4802 驱动低层底稿 |
| **BG5ESN FMO BK4802 V2.00** | `BK4802.c`（寄存器定义/读 RSSI/S-meter/音量/静噪/频偏）、`radio.c`（音频门控）、`raidoConvert.c`（频段合法性与 CTCSS） | **主参考**，最贴近本需求（PY32F030 + BK4802 做 144/430MHz NFM） |
| **VP-Digi** (sq8vps) | STM32 内置 KISS 1200-baud AFSK modem 的 AX.25 编解码思路 | 软件 modem / AX.25 解调参考 |

### 已确认的 BK4802 关键事实

- 半双工 TDD FM/ASK 收发，覆盖 19–33 / 38–66 / 127–177 / 380–530 MHz；2m(144MHz) 与 70cm(430MHz) 均在范围内。
- 控制接口 I2C（7-bit 地址 `0x48`，写地址 `0x90`）；内置 250mW 音频功放、FM 解调器、可读 RSSI/S-meter。
- **频率合成公式**（来源：BK4802N 数据手册 V1.1 + FMO `BK4802.c`，21.25MHz 晶振）：

```
接收：value = round( (f_MHz - IF_MHz) * Ndiv * 2^24 / 21.25 )
IF_MHz = 0.137   // 低中频 137kHz，接收 PLL 锁定在 LO = RF - IF
reg2  = 频段选择字（如 VHF 128–170MHz → 0x2004, Ndiv=12）
reg0  = value 高 16 位
reg1  = value 低 16 位
```

- 音量：reg19 `B13:B12` = FM 解调输出幅度、`B03:B00` = 音量。
- 静噪：reg22 `B07:B00` = 静噪 RSSI 阈值；reg12 `B04` = 中频带宽(200k/1M)、`B07:B05` = 中频增益(3dB/级)。
- 以上音量/解调幅度/中频带宽直接决定 **AFSK 判频音质**，是 Phase 1 必调项。

## 3. 硬件方案

```
天线 ──▶ BK4802（RF 前端 + FM 解调；复用/魔改玩具对讲机板）
          │ I2C (0x48)               │ 解调音频（AF / 功放前级）
          ▼                          ▼
      MCU (STM32)  ◀── ADC / 定时器输入捕获（RC + 偏置）
          │ SPI/8080                     │
          ▼                              ▼
  LCD12864 (ST7567)           蜂鸣器 / 振动 / LED / 背光
          │ I2C/SPI                     │
          ▼                              ▼
  EEPROM / Flash（消息存储）      按键 ×3 + 电源
```

### 电路改造要点

1. 拆掉原机 MCU（刀头/烙铁）。
2. 短接板上滤波器/天线通路（飞线，或拆 LC）。
3. 引出 BK4802 的 SCL/SDA、AF 音频、电源。
4. 焊新 MCU + LCD + 告警件（蜂鸣/振动/LED/背光）。
5. 天线：50Ω 同轴接 SMA，2m 用手台天线。
6. 重新放置电路板、焊接电池盒簧片、装壳。

### 频率规划

目标频率可配置。中国 APRS 常用 VHF（如 144.640/144.645MHz）或 430MHz 段。BK4802 的 127–177MHz（2m）与 380–530MHz（70cm）均可用。上电**默认 144.640MHz**，按键/USART 可改。

**默认频率 144.640MHz 频率字（VHF，Ndiv=12，CRYSTAL=21.25MHz）：**

```
value = round( (144.640 - 0.137) × 12 × 2^24 / 21.25 ) ≈ 1369_049_248
reg2 = 0x2004   // VHF 频段选择字（Ndiv=12）
reg0 = 0x519A   // value 高 16 位
reg1 = 0x08A0   // value 低 16 位
```

> 注：以上按 FMO `nDivCacl` + RX IF 修正后的初始值。上电用
> `value = round((f_MHz - 0.137) × Ndiv × 2^24 / 21.25)` 复算，并在射频台上实测校准（±100Hz 频偏走频偏校准）。

### MCU 选型（已定 STM32F103C8T6）

- **主控：STM32F103C8T6**（64KB Flash / 20KB SRAM）。满足：1KB LCD 帧缓冲、modem、AX.25 帧缓冲、消息记录；硬件 I2C/SPI/USART 齐备。
- 资源估算：modem 零交叉（无浮点）+ HDLC/APRS 解析（~0.5KB RAM）+ LCD 帧缓冲（1KB）+ 消息环形缓冲（RAM 1–2KB）——SRAM 充裕，无需外扩。
- 字库：ST7567 无字库。ASCII 8×16 放内部 Flash；**中文 16×16 建议子集字库**（内部 Flash 受限，见 4.5/6 节；若需全量 GB2312 再外接 W25Q SPI Flash）。
- 至少外设：ADC（1–2 路，音频采样/门控）、定时器输入捕获（零交叉测周期）、SPI（带 CS/A0/RST → ST7567）、GPIO 打拍 I2C（→ BK4802）、若干 GPIO（按键/蜂鸣/LED/背光/振动）。

### 引脚分配表（初版建议，待接线上板核对后固化）

> 现有 MM-Radio 常用命名；实际以 STM32F103C8T6 最小系统板 + 手头板走线为准，Phase 0 确认后锁定。

| 功能 | 引脚（建议） | 说明 |
|---|---|---|
| BK4802 CLK（SCL） | PA4 | 模拟 I2C（MM-Radio 用 GPIOA） |
| BK4802 DATA（SDA） | PA5 | 模拟 I2C |
| BK4802 音频（AF） | PA1 / ADC1_IN1 | FM 解调输出，经 RC 高通 + 偏置 → ADC |
| 门控 / 静噪（可选） | PB0 | 判断是否采样 |
| LCD CS | PB6 | ST7567 SPI |
| LCD CLK | PB3 / SPI1_SCK | ST7567 |
| LCD DATA | PB5 / SPI1_MOSI | ST7567 |
| LCD A0（RS/DC） | PB4 | 0=命令 1=数据 |
| LCD RST | PA8 | 复位 |
| LCD 背光 | PB7 | PWM/GPIO 控亮度 |
| 按键 上/下/选择 | PB12 / PB13 / PB14 | 翻页/确认 |
| 蜂鸣器 | PA6 | 无源蜂鸣（PWM）或带动管 |
| 振动马达 | PA7 | 经三极管/马达驱动 |
| LED | PB15 | 告警/状态 |
| 串口调试/配置 | PA9 / PA10（USART1） | AT 指令或 APRS 数据注入 |
| 电源监测 | PA0 / ADC | 电池电压 |

## 4. 软件方案

采用简单超循环 + 定时器中断，或 FMO 的 SCH51 时间触发调度器。

### 4.1 bk4802.c

- GPIO 位驱动 I2C（MM-Radio/FMO 已有）+ reg0/1/2 写频率。
- reg4~22 接收配置；读 RSSI/S-meter；静噪/音量设置。
- **接收初始化（RX 模式）**：reg4=0x0300（关闭鉴相/前端/中频/接收音频关闭位清除，进入接收）；reg13 `B14=0`（RX 模式），**强制禁用发射**（PTT 仅提示、不写入发射配置）。
- **音频输出（AFSK 用）**：reg19 `B13:B12`=FM 解调输出幅度、`B03:B00`=音量；确保解调输出恒定、不被内部静噪/门控吞掉。
- **中频/接收**：reg12 `B04`=中频带宽、`B07:B05`=中频增益；reg22 `B07:B00`=静噪 RSSI 阈值（APRS 建议放宽或由门控触发采样）。
- 读 RSSI/S-meter：读 SAR ADC 相关寄存器。

### 4.2 音频采集 / modem

- **音频引出（定案）**：从 FM 解调输出（功放前级、reg19 幅度可控）引出，经 RC 高通（约 300Hz）滤掉低频 + 直流偏置至 0~3.3V 中点 → MCU ADC；或用比较器/施密特做正过零 → 定时器输入捕获测周期。
- 采样：ADC 约 19.2kSa/s，或输入捕获直接得到零交叉周期。
- 判频：1200Hz（周期≈833 µs）= mark，2200Hz（周期≈454 µs）= space；按周期分组判决。
- 恢复 NRZI → HDLC 位流；软件 bit/byte 同步（HDLC flag 0x7E 起停）。
- CPU 不足时升级为 Goertzel/相关检测（VP-Digi 思路），保持 ADC 路径不变。

### 4.3 AX.25 / APRS 解析

- HDLC flag `0x7E` 同步 → 位解扰 → 字节解帧 → CRC-16(CCITT) 校验 → 地址（源呼号/路径）与 PID/控制字。
- APRS payload：以 `:` 开头为消息（`<src>:<addressee>:<body>`），取 addressee 与正文；源呼号取帧头。
- 中文字符集转码（UTF-8/GB2312 → 点阵）。

### 4.4 lcd_st7567.c

- SPI 初始化 + 全屏 1KB 帧缓冲 + 点/线/文本绘制 + 滚动/反显/背光。
- ST7567 为纯图形屏，**中文需自备字库**。

### 4.5 font.c

- ASCII 8×16；中文 16×16 建议**子集字库**（常用词/数字/标点 + 可配置常用字，按需在 flash 分区）。
- 全量 GB2312 方案：加 1 片 SPI Nor Flash（W25Q）（见第 6 节）。

### 4.6 alerter.c / ui.c / message_store.c

- 蜂鸣/振动/闪灯告警节奏。
- 会话菜单：待机 / 消息 / 设置 / 频率。
- 历史消息环形缓冲，写入 EEPROM/Flash。

## 5. 实施阶段（里程碑）

| Phase | 目标 | 验收标准 |
|---|---|---|
| **Phase 0 预研与硬件确认** | 拿到 BK4802 板；核对 ST7567 接口与字库；确认音频引出点/引脚走线/天线接口；BOM 定稿、锁定引脚分配 | 决策已定项并落表 |
| **Phase 1 打通射频接收** | 移植 bk4802 驱动；能设频、读 RSSI/S-meter | 能听到 NFM 电台 |
| **Phase 2 音频 → bit 流** | ADC/捕获采集；零交叉 mark/space 判决 | 串口/示波器观察 1200/2200Hz 判决稳定 |
| **Phase 3 AX.25 解码** | HDLC 解帧 + CRC 校验 | 用已知 APRS 帧（仿真/实录）验证 |
| **Phase 4 APRS 消息 + LCD** | ST7567 驱动、字库、显示正文/呼号/时间/RSSI | 真实收台调试通过 |
| **Phase 5 寻呼体验** | 告警、消息存储、翻页、待机界面、设置、频率配置 | 完整 BB 机交互 |
| **Phase 6 结构/电源/整机** | 机壳、电池、天线、低功耗（间断接收/定时唤醒） | 整机测试 |

## 6. 必须提前拍板的关键点

以下均已拍板，剩余为执行期细化项：

1. **LCD 模组 = ST7567（已定）**：纯图形、无字库 → MCU 自备点阵字库。细化项：子集 vs 全量字库（决定是否需要外接 W25Q）。
2. **MCU = STM32F103C8T6（已定）**：细化项：正式引脚分配按实际板走线锁定。
3. **音频引出点 = FM 解调输出（已定）**：细化项：实际 AF 引脚位置与 RC/比较器参数 Phase 1 调通。
4. **仅接收、禁用发射（已定）**：reg13 强制 RX。发射需求二期再评估（需执照 + 发射通路）。
5. **默认频率 = 2m 144.640MHz（已定）**：细化项：频偏/本振校准。

## 7. 风险与应对

| 风险 | 应对 |
|---|---|
| AFSK 判决不稳定（噪音/频偏） | 先零交叉，预留 Goertzel 升级；用 AFC（reg20/21）微调；必要时上相关检测 |
| 中文字库体积 | 子集字库可控（ST7567 无字库）；全量则加外置 W25Q SPI Flash |
| BK4802 内部静噪/音频门控吞 AFSK 首字节 | 关静噪门控、音频恒定输出；或用门控信号触发采样 |
| CPU 时序（modem+HDLC+LCD 刷新） | 定时器中断分片，避免丢位/丢帧 |

## 8. 下一步（先确认，再动手）

决策已落地，进入执行前置项（仍不实现）：

1. 确认手中 BK4802 板实物型号、**FM 解调输出实际引脚/端子**、天线接口；对照上文引脚分配表核对 STM32F103C8T6 接线。
2. 定字库方案：先「子集字库」跑通，再决定是否外接 W25Q SPI Flash 支持全量 GB2312。
3. 固化 `PINMAP.md` + `BK4802_REG.md`（含 RX 模式寄存器默认值与 144.640MHz 频率字）与 `Phase0_Checklist.md`。
4. 之后进入 **Phase 1**：移植 BK4802 驱动，验证设频 + RSSI/S-meter + 音频输出。
