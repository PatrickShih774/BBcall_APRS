# UISkill.md

**BBcall_APRS 设备 UI 设计规范（复古寻呼机 / 128x64 1-bit LCD）**

单文件版 UI skill。约束对象是 STM32F103 + ST7567 128x64 单色点阵上的所有界面，
以及 PC 端 SDL2 模拟器里的同一套界面。**改主页、菜单、列表、详情、弹窗之前先读这一份，再动代码。**

| | |
|---|---|
| 版本 | 1.0（2026-09-12） |
| 来源 | `ui-design`（工程入口 + overlay 契约）、`taste-skill`（Brief Inference / Design Dials / Anti-slop） |
| 适用产物 | 128x64 1-bit 单色 LCD + C（STM32F103C8T6） |
| 实现位置 | 绘图原语 `firmware-stm32porject/Core/Src/lcd_st7567.c`；界面状态机 `simulator/src/ui_harness.c` |
| 字体生成 | `tools/gen_font.py`（8x16）与 `tools/gen_font.py --small`（6x8） |

---

## 0. 适用范围与来源（诚实声明）

上面两个 skill 的目标产物是 **HTML/CSS/JS**。本项目产物是**嵌入式 1-bit LCD**，因此：

**不适用**（不要假装执行）
Track A/B 选择、组件库（shadcn / HeroUI / coss）、WCAG、Core Web Vitals、DOM 成本、
z-index 体系、GSAP / Motion、SEO 与 OG、深色模式 token、响应式断点。

**执行**
Brief Inference、三档 Design Dials、Anti-slop 硬规则、Pre-flight 检查表，
并按第 3 节的映射表把 Web 工程护栏换成嵌入式等价物。

**冲突仲裁**
风格冲突以 `taste-skill` 为准；工程正确性（刷新成本、时序、RAM/Flash、可读性、闪烁）以硬件约束为准。

---

## 1. Brief Inference（先读题，再动手）

### 1.1 Design Read（本项目固定，改版时重新声明一次）

> 读作：**复古手持寻呼机（BB 机）设备 UI**，用户是单人业余无线电操作者，
> 采用 **90 年代末点阵寻呼机（Motorola Advisor 一类）**的视觉语言，
> 倾向 1-bit 单色系统：**状态图标栏 + 反显选择条 + 大字号时钟**。

### 1.2 Anti-default（每次都要自问）

1-bit 世界最容易滑向的四种默认，一律禁止：

1. **什么都套一个 1px 方框**。这是 Web「卡片默认」的等价物，也是最偷懒的一招；
2. **每个列表行都加箭头或项目符号**。1-bit 里箭头占一整列，而且反显已经表达了选中；
3. **用装饰性状态点 / 图标填充空白**。图标只在有真实语义时出现（见 4.5）；
4. **搬 Web 视觉习惯**：圆角、阴影、渐变、留白呼吸感、居中对称大标题。

正确做法是**唯一 chrome 系统**（第 4.1 节）：状态栏 + 细线 + 行网格 + 滚动轨，只此一套。

---

## 2. 三档 Design Dials

| Dial | 取值 | 依据 |
|---|---|---|
| `DESIGN_VARIANCE` | **6** | 设备 UI 要肌肉记忆，不能乱排；但主页用「大时钟 + 偏置信息行」的非对称构图，不做居中对称 |
| `MOTION_INTENSITY` | **2** | 嵌入式：刷新慢、耗电、闪屏刺眼。只保留一处有动机的动画 |
| `VISUAL_DENSITY` | **7** | 寻呼机本就是信息密度设备；按 taste 第 7 节，密度 7 以上要求 **1px 细线分隔数据、不用卡片盒** |

### 2.1 允许的动效（白名单）

- **主页大时钟冒号 0.5s 闪烁**。动机：设备存活反馈。
- 除它之外，任何动画都要在代码注释里写清「它传达什么」（层级 / 叙事 / 反馈 / 状态变化），
  写不出来就不加。**没有第 2 个白名单条目。**

### 2.2 刷新纪律（嵌入式版的 `prefers-reduced-motion`）

- 只在必要时重绘：`ui_tick()` 仅当当前屏幕是 `home` 时才重绘（大时钟需要）。
- 不做全屏闪烁、不做整屏反显切换。
- 无动画路径必须存在：`--selftest` 单帧渲染即可出图，不依赖任何动画。

---

## 3. Web 规则 → 1-bit LCD 等价物

| Web 原规则 | 本项目等价物 |
|---|---|
| 颜色对比 / 单一强调色 | 只有开与关两态。层级靠**面积、墨量、反显**；「强调色」只能是反显 |
| `prefers-reduced-motion` | **刷新成本与闪烁**：只重绘必要区域，不做全屏闪（见 2.2） |
| Dark mode（主题锁） | 屏的**两种极性**：背光正显 / 反显负片。每屏锁定一种基调，不中途翻转 |
| Responsive breakpoints / 移动端折叠 | 固定 128x64 网格；「密度档」= 6x8 与 8x16 两套字体 |
| 「禁止手搓 SVG 图标」 | 必须自绘 1-bit 图标。等价纪律：**统一 8x8 网格、统一 1px 线宽、单一图标家族** |
| 禁止 em-dash | 字模只覆盖 `0x20-0x7F`，`U+2014` 在设备上**根本无法渲染**，结构性满足（本文件自身也零出现，仅以码位提及） |
| 长列表不得全部铺开 | 6 行窗口 + 比例滚动轨 |
| 空状态 / 加载状态 | 必须有空状态（`no message yet` / `no position yet`）；无异步，故无加载态 |
| 不得出现假精确数字 | 状态栏信号格与电台页数值必须来自真实来源（日志里的 `S=` / `R19=`），没有采样电路就不画电池 |

---

## 4. 固定设计系统

### 4.1 唯一 chrome（不允许出现第二套）

```text
y0..7     顶部状态栏（反显）：左 = 屏幕名（6x8 挖字），
                              右 = [信号格 8px] 3px [静音 8px] 3px [未读数] 3px [信封 8px]
y8        1px 细线
y10..56   内容区，6x8 行网格 y = 10 / 18 / 26 / 34 / 42 / 50
x124..127 滚动轨（仅列表溢出时出现）
```

- 状态栏右对齐从右往左排，元素之间固定 3px 间隙。
- 内容区**不再套任何边框**。整屏只有状态栏是反显块 + 一条细线。
- 例外只有一处：模态弹窗（第 5.7 节）允许填充块 + 内嵌 1px 框。

### 4.2 网格与字号

| 字体 | 尺寸 | 容量 | 用途 |
|---|---|---|---|
| `font8x16.h` | 8x16 | 16 字符/行 | 大标题、大时钟（2 倍放大 = 16x32） |
| `font6x8.h` | 6x8 | 21 字符/行 | 状态栏、列表、正文、菜单 |

- 6x8 字形实际落在行内第 1..6 行（`y+1 .. y+6`），所以 `y=50` 行像素在 51..56，底部留 7px 余量。
- **居中与右对齐必须吸附到字符栅格**：8x16 对齐到 8 的整数倍，6x8 对齐到 6 的整数倍。
  半格偏移会让点阵边缘发糊（这是踩过的坑，见 `text8_center` / `text6_right` 注释）。
- 分隔线固定：状态栏下 `y=8`；主页第二条 `y=44`。

### 4.3 极性（Theme Lock）

- 每屏锁定一种基调，不在同一屏里来回翻转。
- 反显只用于四种场合：**状态栏、选中行、模态弹窗、强调文字**。

### 4.4 「强调色」= 反显

- 一屏之内，反显只承担**一个**语义。既当选中又当装饰，等于没有强调色。
- 反显实现：`lcd_fill_rect(..., 1)` 铺底，再用 `ink=0` 绘制文字 / 图标（挖字）。

### 4.5 图标家族

- 统一 **8x8 网格**、**1px 线宽**、纯几何风格，不用位图字模。
- 现有 8 个：`信号格`、`信封`、`静音喇叭`、`地图针`、`天线`、`对比度`、`信息 i`、`电源`。
- 新图标规则：8x8 内绘制、四周留 1px 边距、**必须带 `ink` 参数**（反显时传 0 才能挖字）、
  只用 `lcd_pixel / lcd_hline / lcd_vline / lcd_rect / lcd_fill_rect`。
- **有真实语义才画**。板子上没有电池采样电路，所以电池位保留但**留空**。
- 例外的唯一一处：Messenger 首页的 **24x24 大图标**（启动器不是数据列表，需要辨识度），见第 11 节。

### 4.6 选择态与滚动轨

- 数据列表的选择态一律**整行反显**，不加 `>` 箭头。
- 启动器（Messenger 首页）用**标签胶囊反显**：反显范围不得覆盖右侧大图标。
- 列表溢出时在 `x=126..127` 画 2px 宽比例滚动块：
  `track = vis*8 - 2`，`hh = max(3, track*vis/count)`，`y = track_top + (track-hh)*top/(count-vis)`。

### 4.7 数据结构（界面状态机约定）

- 收件箱条目字段：`kind`（MSG / POS / MIC-E / APRS）、`src`、`dst`、`path`、`title`（单行摘要）、
  `body`（完整正文）、`read`（未读标志）、`relay` / `fixed` / `repeat`（来源标记）。
- **列表顺序 = 最新在上**（新帧插队首），选中项默认停在队首。
- 未读：新入箱置未读，Enter 打开后置已读；状态栏与菜单的数值随之更新。
- 删除两步走：Delete 弹确认，OK 才真删（同时标记已读），BACK 取消。
- 重复包抑制：同源同内容 60s 内只入箱一次（与固件 `bbcall_app.c` 一致）。

---

## 5. 屏幕清单与版面模板

### 5.1 `boot` 开机

```text
+--------------------------------+
|                                |
|      B B C A L L   (2 倍放大)   |   y=6..37, x=16
|                                |
|        APRS RX PAGER           |   y=44
|           FW v0.3              |   y=54
+--------------------------------+
```
无状态栏（唯一不用 chrome 的屏幕，因为它是开机瞬间）。

### 5.2 `home` 主页

```text
y0  +------------------------------+
    | HOME      [] 3 [] (反显状态栏) |
y8  +------------------------------+
    |           0 1 : 1 2           |  16x32 大时钟, x=24, y=11
    | UP                            |  y=23 明示这是开机计时
y44 +------------------------------+
    | 144.640 MHz          RX 13   |  y=46
    | BD4BE  MIC-E     3111.28     |  y=54
    +------------------------------+
```
- 大时钟 = 开机计时 `HH:MM`（无 RTC）。左侧 `UP` 是诚实标注，不是装饰。
- 冒号 0.5s 闪烁（唯一动效）。

### 5.3 `menu` 菜单

```text
y0  | MENU        [] 3 [] |
y8  +---------------------+
    |[图标] Inbox      13  |  <- 选中行整行反显
    |[图标] Positions      |
    |[图标] Radio          |
    |[图标] Contrast       |
    |[图标] Backlight      |
    |[图标] About          |
```
- 行格式：图标 x=2..9，标签 x=12，数值右对齐吸附 6px 栅格。
- 6 项以内不画滚动轨；超出时用滚动轨而不是缩小字号。
- 每项必须对应**真实行为**，不做占位菜单项。当前行为：
  `Inbox` 打开全部、`Positions` 打开位置过滤、`Radio` 电台页、
  `Contrast` 真机改 ST7567 `0x81`（模拟器空操作）、`Backlight` 背光、`About` 信息页。

### 5.4 `inbox` 列表

```text
y0  | INBOX 1/13   [] 3 [] |
y8  +-----------------------+
    |BD4BE  C 3111.28N 1*   |  <- 选中行反显；* = 未读
    |BD4SDX P 3054.31N 1*   |
    |BH4FSK C 3037.90N 1*   |
    |BG4AIZ C 3137.15N 1*   |
    |BD4BE  C 3111.28N 1*   |
    |BI4BKX C 3116.01N 1*   |
```
- 行格式 `呼号(6) 类型(1) 摘要(10) 未读(1)`，共 20 字符，右侧留滚动轨。
- 标题栏 `1/13` 是真实导航状态，不是装饰性序号。

### 5.5 `detail` 详情

```text
y0  | MIC-E BD4BE   [] 3 [] |
y8  +-----------------------+
    |3111.28N 12125.77E     |  正文 5 行 (y=10..42)
    |M0: Off Duty 2km/h     |
    |223 `"5f} $            |
    |                       |
    |1/1 RELAY BH4ERY*,WID  |  y=50 页脚
```
- 页脚优先级：`RELAY <路径>` > `FIX` > `REP`。
- 正文 20 字符宽，5 行/页，超出翻页。

### 5.6 `radio` / `about` 信息页

```text
y0  | RADIO         [] 3 [] |      y0  | ABOUT        [] 3 [] |
y8  +-----------------------+      y8  +---------------------+
    |144.640 MHz  S4        |          |BBCALL_APRS          |
    |RSSI 72    SNR 19      |          |FW v0.3  GPL-3.0     |
    |AFC  14    EXN 315     |          |STM32F103C8T6        |
    |RX 13  DUP 0            |          |ST7567 128x64 LCD    |
    |audio: on              |          |BK4802P 21.25MHz IF  |
    |BACK=MENU              |          |144.640 MHz APRS     |
```

### 5.7 `confirm` 删除确认（唯一允许第二个框的地方）

```text
        +------------------+
        |  DELETE MESSAGE? |   <- 填充块 + 内嵌 1px 框 + 反显文字
        |  OK=YES  BACK=NO |
        +------------------+
```
- 位置：填充 `x6..121 / y18..45`，内框 `x8..119 / y20..43`，文字居中吸附 6px 栅格。

### 5.8 `pattern` 字体样张

棋盘格 + 两套字体全字符（`0x20-0x7F`）。换字体或换屏后用它一眼看出缺字。

---

## 6. 硬规则

### 6.1 必须

1. 所有界面走第 4.1 节的唯一 chrome（例外只有 `boot` 与模态）。
2. 文字吸附字符栅格；绝不放半格。
3. 选择态用整行反显。
4. 列表溢出必须给滚动轨。
5. 空状态必须给（`no message yet` / `no position yet`）。
6. 屏幕上的每个数字都要有真实来源。
7. 反显块内的图标与文字必须用 `ink=0` 挖字，不能直接画在同色底上。

### 6.2 禁止

1. 内容区套方框 / 卡片边框（模态除外）。
2. 列表行加箭头、项目符号、装饰点。
3. 同一屏出现两种极性的反显块。
4. 装饰性动画；动效白名单只有冒号闪烁（第 2.1 节）。
5. 假数据：假电量、假 RSSI、假时间。
6. 占位菜单项（点了没反应、也不是明确的功能说明）。
7. 把版本号写在主页（版本只在 `boot` / `about`）。
8. 混用图标风格（线宽、网格、圆角不一致）。

### 6.3 文案

- 全部 ASCII（`0x20-0x7F`），字模不支持中文；不要写中文界面文案。
- 用祈使 / 事实语气，短名词优先：`HOME`、`MENU`、`INBOX 1/13`、`no message yet`、`BACK=MENU`。
- 不用营销腔、不用 emoji、不用「欢迎」「感谢」这类对设备无意义的句子。
- 写完逐个字符串复读一遍，不通顺就改（踩过的例子：`back: back` 改成 `BACK=MENU`）。

---

## 7. Pre-flight 检查表

改动 UI 后逐条过，**不适用的项写明 N/A 及原因**。

- [ ] Design Read 已声明（第 1.1 节，改版时更新）
- [ ] Dials 有明确取值与理由，不是默认值（第 2 节）
- [ ] 只用唯一 chrome；`boot` / 模态之外没有第二个框（第 4.1 节）
- [ ] 每屏只有一种极性基调，反显只承担一个语义（第 4.3 / 4.4 节）
- [ ] 所有文字吸附字符栅格（8 或 6 的整数倍）
- [ ] 列表溢出有滚动轨；空状态有文案
- [ ] 图标：8x8、1px 线宽、带 `ink` 参数、有真实语义
- [ ] 无假数据（电量 / RSSI / 时间都有来源，或明确留空）
- [ ] 无占位菜单项
- [ ] 动效不超过白名单；新增动效能一句话说清动机
- [ ] 文案逐条复读，ASCII，无营销腔
- [ ] 根目录 `UISkill.md` 的屏幕模板与实际渲染一致（改了版面要同步）
- [ ] 没有 em-dash（字模渲不出来，写进代码也是废字符）
- [ ] 固件回归编译通过，`text` 体积变化有解释
- [ ] 用第 8 节的方法逐屏读回验证，而不是"看着差不多"

---

## 8. 验证方法（不许只看截图）

1. **无窗口自检**（确定性，可重复）：

   ```powershell
   powershell -ExecutionPolicy Bypass -File simulator\build_win.ps1
   cd simulator\build-win
   .\bbcall_sim.exe --selftest --scale 3 --clock 4337 --screen home --out home.bmp
   ```

   可用 `--screen`：`boot` / `home` / `menu` / `inbox` / `detail` / `radio` / `about` /
   `confirm` / `pattern`。

2. **逐屏读屏验证**：按像素距离做模板匹配（支持反显行与 2 倍放大字形），
   把 128x64 位图读回成字符，和期望文本比对。工具脚本见提交历史里的 `bbcall_ocr*.py` 思路，
   核心是：每个字符格对两套字模求 Hamming 距离取最小，每行在「正显 / 反显」两种解释里取总距离更小的那个。

3. **真数据回放**：`--replay tools\sample_aprs_log.txt`（13 个真实帧 + 每帧之前最近的真实
   `S=` / `R19=` 状态行）。状态栏信号格与 `radio` 页数值必须与日志原值一致。
   音频链路：`--wav tools\test_aprs_144.wav`（走固件 `modem.c` 解调）。

4. **固件回归**：`firmware-stm32porject/Debug` 下 `make -j4 all`，确认 `text` 体积变化可解释。

5. **逐像素比对才算通过**：对每个新屏幕，用 Python 按同一套字模渲染一份"期望画面"，
   与 `--selftest` 输出的 BMP 做逐像素 diff，**差异必须为 0**。读屏 OCR 会骗人（行网格不同就会读错），
   逐像素 diff 不会。Messenger 五屏就是这么验的。

---

## 9. 移植到真机（LCD 焊上之后）

当前 UI 状态机住在 `simulator/src/ui_harness.c`，固件自己的画面（`bbcall_app.c`，
`BBCALL_LCD_ENABLED=0` 休眠中）还是旧的简单版。移植步骤：

1. 新建 `firmware-stm32porject/Core/Src/bbcall_ui.c` + `.h`，把 `ui_harness.c` 的模型与屏幕搬过去；
   注意在 STM32CubeIDE 里刷新项目，让新文件进入构建（命令行 `make` 不会自动收录新文件）。
2. 界面里对模拟器的依赖只有三处，替换即可：
   `printf` 调试输出、`--clock/--screen` 之类的命令行入口、以及 `sim_*` 数据源。
3. 数据来源换成固件现有路径：`modem_get_frame()` 出帧 → `bbcall_ui_feed_ax25()`；
   状态栏信号格接 `bk4802_get_smeter()`，静音接音频静音状态。
4. 关注 RAM：`ui_item_t` 24 条 × (标题 + 正文 128B) 约 4KB。
   若紧张，先减 `UI_INBOX_MAX`，再考虑把 `body` 换成外置 Flash 引用。
5. `bbc_app_loop()` 里保持"只在需要时重绘"（第 2.2 节），不要把 `draw_home()` 放进每轮循环。

---

## 10. 已知留白（诚实标注，不要偷偷补假数据）

| 项 | 现状 | 补齐条件 |
|---|---|---|
| 电池图标 | 位置保留，未启用 | 接电池分压到 ADC 后 |
| 主页大时钟 | 开机计时（无 RTC），左侧标 `UP` | 加 RTC（如 DS3231）后改为墙上时钟并去掉 `UP` |
| `Contrast` 菜单 | 真机改 ST7567 `0x81` 值；模拟器空操作 | 模拟器加对比度可视化（可选） |
| 中文显示 | 不支持，字模只有 ASCII | 子集化 GB2312 字库，或外置 Flash 全字库 |
| 未读持久化 | 断电即丢 | 收件箱写外部 Flash / EEPROM |

---

## 11. Messenger 界面族（版面参考 GOGUFW）

参考项目：[Gogu-Qs/GOGUFW-UV-K1-Messenger](https://github.com/Gogu-Qs/GOGUFW-UV-K1-Messenger)
（Apache-2.0，泉盛 UV-K1 / UV-K5 V3 定制固件）。它的 LCD 同样是 **128x64 单色**，
是目前能找到的、与本项目硬件最接近的寻呼 / 消息类界面参考。本节的取舍依据都在第 3 节的映射表内。

### 11.1 采用的内容规范

| 项 | 值 | 说明 |
|---|---|---|
| 正文长度 | 36 字符 | `MSG_TEXT_MAX`，与参考一致，便于两端行为可比 |
| 容量 | Inbox 16 / Sent 8 / Drafts 8 | 参考值 |
| 年龄列 | `NOW` / `12m` / `3h` | 三档，不显示秒 |
| 未读标记 | `*` | 行首 |
| 送达标记 | `+` 已确认 / `x` 失败 / `-` 待确认 | 用于 Sent |
| 列表行格式 | 标记 + 正文预览 + 右对齐年龄 | 本项目 20 字符：`%c%-14.14s%5s` |
| 去重 | 按 `(from, id)` | 同一发送方同一 id 只入箱一次 |
| ACK | 正文 `ackNNN` 匹配 Sent 的 id | 支持多来源（最多 3 个），记录 ACK 来源呼号 |
| 点状分隔线 | 1 实 3 空 | 参考项目明确写了"比实线轻，不压正文" |
| 右对齐计数 | 右侧留安全边距 | 参考项目实测最后一格贴边会换行 |
| 组包计数 | `已输入/36` | 与 `NEW MESSAGE` 同一行 |

### 11.2 有意偏离（本项目 chrome 优先）

| 参考做法 | 本项目做法 | 理由 |
|---|---|---|
| 顶部居中标题行（占 8px） | 标题放进**反显状态栏** | 我们已有状态栏，再加一条标题等于白扔 8px，密度 7 不允许 |
| 首页 4 项 + 右侧大图标 | **保留**（唯一例外） | 首页是启动器不是数据列表，大图标提供辨识度 |
| 选中态 = 文本胶囊反显 | 数据列表整行反显；**首页用标签胶囊反显** | 首页右侧有大图标，整行反显会把图标埋掉 |
| 6x7 字体、7px 间距 | 6x8 字体、6px 间距 | 沿用本项目字模，不引入第二套 |
| 独立 HEARD / Range Check 页 | HEARD 复用条目列表；Range Check 未实现 | 本项目纯接收，Range Check 需要双向 |

### 11.3 屏幕

| 屏幕 | 内容 |
|---|---|
| `messenger` | 4 项启动器：INBOX / HEARD / COMPOSE / SENT；选中项右侧 24x24 大图标；右对齐计数（INBOX 显示 `*未读`） |
| `msginbox` | 6 行，最新在上：`*` + 正文预览 14 字符 + 年龄 5 字符右对齐 |
| `msgsent` | 同上，行首换成 `+` / `x` / `-` 送达状态 |
| `msgread` | `FROM:`/`TO:` + 年龄 → 点状分隔 → 正文 4 行（20 字符折行）→ 点状分隔 → 页脚 `REPLY`/`DEL`；Sent 侧为 `RESEND` + ACK 来源 |
| `compose` | `NEW MESSAGE` + `n/36` → 点状分隔 → 正文 + 光标 `_` → 点状分隔 → `SAVE DRAFT` + **`TX OFF`** |

数据模型在 `simulator/src/msg_store.c`（纯数据、不绘图，便于整体移植进固件）；
屏幕在 `simulator/src/ui_harness.c`。

### 11.4 诚实约束

本项目**仅接收、禁止发射**（硬件与合规决定，见 README 第 1 节）。因此：

- `COMPOSE` 只保存草稿，页脚明确写 `TX OFF`，**不做能点却发不出去的假按钮**；
- `SENT` 平时为空，只有将来打开发射能力后才会自然填充；模拟器用 `--demo` 注入演示数据；
- 收到 `ackNNN` 时更新 Sent 的送达状态，这条路径现在就工作。